.filters
  .row
    .col.instruction.s2
      Filter traces
    = form_tag(url_for(controller: 'pages', action: 'home'), method: :get, class: 'col s10') do
      .row
        .input-field.col.s10
          = select_tag(:segment, options_for_select(DatasetSegment::SEGMENTS, params[:segment]))
          %label
            Dataset segment
        .input-field.col.s2
          = submit_tag 'Filter', class: 'waves-effect waves-light btn'

