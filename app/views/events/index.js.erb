$("<%=j render(partial: 'index') %>").appendTo($(".infinite-list .events"));

<% if @events.last_page? %>
  $('.pagination').html("That's all, folks!");
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@events, 'Next Page', remote: true) %>");
<% end %>
